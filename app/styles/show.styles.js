import { StyleSheet } from 'react-native';

const showStyles = StyleSheet.create({
	container: {
		padding: 10
	},
	image: {
		resizeMode: 'cover',
	},
	marginContent: {
		marginTop: 10,
		marginBottom: 10
	}
});

module.exports = showStyles;